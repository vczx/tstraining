let num1;
const num2 = 10;

//with const we cant assign to a new obejct, but can change the value
const obj1 ={
    name:"vincent"
};

console.log(obj1.name);

obj1.name = "Vincent 2"

console.log(obj1.name);
